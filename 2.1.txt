// Задание: необходимо реализовать функцию, возвращающую значение всех аргументов, если тип аргумента - число. Вызов ВСЕГДА 5

функция  количество ( число )  {
  если  ( тип  числа  !==  'число' )  {
     количество возвратов ;
  }

  пусть  сумма  =  число ;

  функция  nextCount ( nextNum )  {
    если  ( typeof  nextNum  ===  'число' )  {
      сумма  +=  следующееЧисло ;
    }
    вернуть  следующий счетчик ;
  }

  следующийСчетчик . toString  =  функция  ( )  {
     сумма возврата ;
  } ;

  вернуть  следующий счетчик ;
}

консоль . журнал ( + количество ( 3 ) ( 2 ) ( 1 ) ( 5 ) ( 7 ) ) ;
консоль . журнал ( + количество ( 6 ) ( 17 ) ( 3 ) ( 5 ) ( 7 ) ) ;
консоль . log ( + count ( 3 ) ( null ) ( 1 ) ( ' asad ' ) ( ) ) ;